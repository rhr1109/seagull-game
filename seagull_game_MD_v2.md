# 🚬🐦 프로젝트 "끄어억(KKEUH-EOK)"
## 부둣가 골초 갈매기 시뮬레이터 — 마스터 문서 v2

> **한 줄 컨셉**: 부둣가에 한심한 갈매기 한 마리가 산다. 당신은 그를 키운다. 그게 다다.
> **장르**: 부조리 육성 로그라이트 (Absurd Life-sim Roguelite) / 도트 2D
> **톤 키워드**: `뻔뻔함` · `허세` · `무논리` · `허무` · `짠내`
> **태도**: 이 게임은 흡연에 대해 어떤 입장도 없다.

---

## 0. 장르 정의

### 0.1 왜 "부조리 육성 로그라이트"인가

본 게임은 단일 장르가 아니라 세 가지 장르의 하이브리드다.

| 구성 요소 | 역할 | 비교 작품 |
|----------|------|----------|
| 육성 시뮬레이션 (메인 뼈대) | 일일 스탯 관리, 30일 수명 사이클 | 다마고치, Pou |
| 로그라이트 (회차 동기) | 죽음 → 메타 진행 → 재도전 | Cult of the Lamb, Hades |
| 자원 관리 어드벤처 (일일 루프) | 시간대별 사냥터, 담배 수급, 선택지 | Reigns, Don't Starve |

### 0.2 로그라이트 적용 방식

본 게임의 로그라이트 정체성은 **"세이브 스캠 차단 + 영구 죽음"이 아니라 "메타 진행 + 엔딩 수집"에 있다**. 플레이어는 자유롭게 분기를 탐색할 수 있고, 그 시도들이 영구 메타 데이터에 누적된다.

**자유로운 회차 탐색**
- 갈매기는 평균 30일 만에 죽거나 사라진다
- 단, 세이브 슬롯 3개 + 자유로운 로드 허용 (다양한 엔딩 시도용)
- 죽음의 무게보다 **"이번엔 뭐가 나올까"**의 호기심이 핵심

**메타 진행 (Meta-progression)** — 회차 사이 영구 보존
- 해금된 엔딩 앨범 (10/10)
- 해금된 시작 외형 (정장 갈매기 등)
- 해금된 사냥터 (도시 옥상 등)
- 누적 통계 (총 흡연량, 총 도달 엔딩, 최장 수명)

**1회차 분량**
- 40~90분 권장
- 엔딩에 도달하면 다음 회차 동기 부여 (해금/통계)

### 0.3 무엇이 아닌가
- 자유도 높은 오픈월드 ❌
- 실시간 액션 ❌
- 스토리 중심 내러티브 어드벤처 ❌
- F2P 가챠 ❌

---

## 1. 자료조사자

### 1.1 레퍼런스
| 작품 | 훔쳐올 것 |
|------|----------|
| Happy Tree Friends | 귀여움과 잔혹함의 충돌, 사과 없는 태도 |
| Don't Hug Me I'm Scared | 의미 없는 부조리에서 오는 불쾌한 웃음 |
| Goose Game | 민폐 동물 IP의 상품성 |
| Cuphead | 1930년대 카툰 + 짜증나는 난이도 |
| 다마고치 | 죽음 엔딩의 클래식한 무게감 |

### 1.2 갈매기 행동학
- 도시 갈매기는 인간 음식에 적응한 도시형 종족
- 영국 브라이튼·부산에서 꽁초 훔치는 갈매기 실제 다수 목격
- 평균적으로 한심하고, 시끄럽고, 쓰레기를 먹는다
- 우리 주인공은 그중에서도 더 한심하다

### 1.3 타깃
- 1차: 인디게임 팬, 다크유머 좋아하는 20~35세
- 2차: 짤 만들 거리 찾는 스트리머
- 3차: 〈Cult of the Lamb〉 좋아한 사람들

### 1.4 시장 공석
"동물 + 부조리 + 한심함" 조합은 국내 인디 시장에 거의 없음. 〈Goose Game〉이 증명한 민폐 동물 시장은 아직 한국에서 본격 개척 안 됨.

---

## 2. 브랜드기획자

### 2.1 브랜드 한 줄
**"끼룩이 아니라 끄어억"**

### 2.2 메인 카피 후보
- "오늘도 한 대만…"
- "갈매기에게도 사정이 있다"
- "당신의 새는 오늘 무엇을 피웁니까"
- "부두에 사는 새는 다 사연이 있다"

### 2.3 캐릭터: "꽁초(Kkongcho)"
- 누렇게 뜬 깃털, 충혈된 눈, 부리에 항상 꽁초
- 평생 일해본 적 없음
- 친구가 있다고 주장하지만 확인된 바 없음
- 시그니처 사운드: "끄어억…"

### 2.4 마케팅
**출시 전**: 인스타/X에 갈매기 짤 시리즈. "월요일 출근하는 갈매기", "전세 빼앗긴 갈매기" 같은 인간 풍자 + 갈매기 합성. 트레일러는 다큐 패러디 ("부둣가의 무명 새들").

**출시 후**: 스트리머 키 무료 배포. "내 갈매기 자랑하기" 트위터 챌린지. 굿즈는 누런 깃털 인형, 꽁초 모양 키링 (실제 라이터 X — 굳이 선 넘을 필요 없음).

**배포 / 가격 모델** (단일 HTML 기반)
- **itch.io**: "이름 만큼 지불(Pay what you want)" 모델, 권장 가격 $2~5
- **자체 웹사이트**: 무료 플레이 + 후원 버튼 (Buy Me a Coffee, 토스 후원)
- **다운로드 .html 파일**: 오프라인 보관용 무료 배포
- **광고 / 인앱결제 ❌**: 단일 HTML 정체성 유지
- 모바일 앱스토어 출시는 추후 PWA(Progressive Web App) 또는 Capacitor 래핑으로 검토

### 2.5 등급: 청소년이용불가 (필수)

본 게임은 **청소년이용불가 등급으로 출시한다**. 이는 회피해야 할 결과가 아니라 **사전에 확정된 마케팅 자산**이다.

**적용 위치**
- itch.io / 자체 웹페이지 썸네일 우측 하단에 청소년이용불가 마크 노출
- 게임 첫 로드 시 청소년이용불가 표기 + 18세 이상 확인 다이얼로그 (확인 시 `localStorage`에 저장)
- 공식 트레일러 시작/종료 시 청소년이용불가 마크
- SNS 공식 계정 프로필 / 핀고정 게시물에 표기
- 굿즈 패키지에도 표기
- 엔딩 공유 카드 워터마크에도 표기

**활용 전략**
- "청소년이용불가" 자체를 브랜드 톤의 일부로 사용 — 검열 없는 자유로운 톤의 보증서로 포지셔닝
- 스토어 설명 첫 줄: "청소년이용불가. 그래야 할 만한 이유가 있다."
- 18세 미만 확인 다이얼로그 우회 시 자기 책임 명시 (웹 게임 한계상 강제 차단 불가)

---

## 3. 게임기획자

### 3.1 핵심 루프
```
[하루 시작] → [컨디션] → [활동] → [이벤트]
     ↑                              ↓
  [수면/사망] ← [밤 활동] ← [금단증상]
```

### 3.2 5각형 스탯
| 스탯 | 설명 | 핵심 갈등 |
|------|------|----------|
| 니코틴 욕구 | 갈망 정도 | 안 채우면 행동 페널티 |
| 폐 건강 | 누적 손상 | 0 되면 그냥 죽음 |
| 비행 능력 | 사냥 효율 | 폐 망가지면 같이 망가짐 |
| 평판 | 갈매기 사회 신뢰 | 무리 행동 가능 여부 |
| 스트레스 | 정신 상태 | 흡연으로만 풀림 (이게 함정) |

### 3.3 담배 수급
**시간대별 사냥터**
| 시간 | 장소 | 등급 | 위험도 |
|------|------|------|-------|
| 아침 | 해변 관광객 가방 | C | ★★ |
| 점심 | 공원 흡연 구역 꽁초 | F~D | ★ |
| 저녁 | 술집 앞 골목 | B | ★★★ |
| 심야 | 클럽 뒷골목, 부둣가 창고 | A~S | ★★★★ |
| 비/눈 | 어디든 | 라이터 점화 실패 가능 | +★ |

**담배 등급**
| 등급 | 종류 | 만족 | 폐 손상 |
|------|------|------|---------|
| F | 비에 젖은 꽁초 | 10 | 5 |
| D | 길거리 꽁초 | 25 | 8 |
| C | 편의점 절도 | 50 | 12 |
| B | 정품 한 갑 | 75 | 15 |
| A | 외제 시가 | 95 | 20 |
| S | 전설의 쿠바산 | 150 | 35 |

### 3.4 이벤트

**라이벌 갈매기 "삥 뜯기"**
큰형 갈매기가 등장해 한 대 빌려달라고 함. 양보(평판 ↑) / 거절(전투) / 거짓말(들키면 평판 -20).

**관광객 도시락 강탈**
QTE 미니게임. 성공하면 음식+담배, 실패하면 부상.

**비 오는 날 라이터 점화**
화면 흔들림 + 클릭 타이밍. 실패하면 욕구 폭발.

**돌고래의 충고 (히든)**
부둣가 돌고래가 가끔 등장해 인생 조언 함. 무시하면 나타나지 않음. 들으면 아무것도 안 바뀜.

**"이상한 새가 다가옴"**
검은 정장 갈매기가 다가와 명함을 줌. 받으면 며칠 뒤 무슨 일이 생김. 안 받으면 안 생김.

**금연 도전 (있긴 있음)**
폐 건강 30% 이하에서 발동 가능. 7일 버티면 폐 회복 + "전설의 금연자" 타이틀. 단, 스트레스 폭주로 다른 사고 가능.

### 3.5 엔딩 (총 10종, 교훈 ❌)

각 엔딩은 도달 조건과 **개별 해금 보상**을 가진다. 단순 컬렉션이 아니라, 어떤 엔딩을 보느냐에 따라 다음 회차의 플레이 스타일이 바뀐다.

| # | 엔딩 | 도달 조건 | 개별 해금 보상 |
|---|------|----------|---------------|
| 1 | 폐 망가져 죽음 | 폐 건강 0 도달 | **코스튬: 환자복 갈매기** (병원 가운, 링거 부착) |
| 2 | 전설의 골초 | S급 시가 100개 흡연 | **코스튬: 시가 마스터** (실크 가운, 베레모) + 시작 시 S급 1개 지급 |
| 3 | 갈매기 마피아 보스 | 평판 MAX + 부하 갈매기 5마리 | **코스튬: 정장 갈매기** (검은 정장, 선글라스) + 시작 평판 +20 |
| 4 | 약물 과다 | 시가 한 번에 5개 흡연 | **코스튬: 좀비 갈매기** (충혈 눈, 헝클어진 깃털) |
| 5 | 인간 입양 | 인간 호감도 MAX 상태로 부상 | **코스튬: 가정용 갈매기** (목줄 + 이름표) + 시작 시 음식 5개 지급 |
| 6 | 갑자기 철학자가 됨 | 30일 동안 흡연 없이 평판 50 이상 유지 | **코스튬: 사색가** (안경 + 책) + 스트레스 자연 감소율 +10% |
| 7 | 담배 회사 CEO와 결혼 | 특정 이벤트 트리거 후 A급 이상 50개 흡연 | **코스튬: 웨딩 갈매기** (턱시도 + 부토니에) + 시작 자금 보너스 |
| 8 | 그냥 어느 날 사라짐 | 평판 0 + 14일 연속 동료와 상호작용 없음 | **코스튬: 안개 갈매기** (반투명 효과) — 다른 갈매기에게 무시당함 |
| 9 | 돌고래와 떠남 (히든) | 돌고래 충고 5회 청취 + 금연 7일 | **코스튬: 항해사 갈매기** (선원 모자) + 신규 사냥터 "원해" 해금 |
| 10 | 평범한 삶 (히든) | 모든 스탯을 50~70 사이로 30일 유지 | **코스튬: 회사원 갈매기** (와이셔츠 + 넥타이) + 시작 시 모든 스탯 +10 |

> **밸런스 원칙**: 보상은 다음 회차를 *더 쉽게* 만들지 않는다. **다른 플레이를 시도해보고 싶게** 만든다. 예: 시가 마스터 코스튬은 S급 시가로 다시 도전할 동기를, 사색가는 금연 플레이를 권유한다.

### 3.6 엔딩 앨범 시스템

플레이어의 회차 동기를 만드는 핵심 메타 콘텐츠. 죽이고 살리고 사라지게 만들 이유.

**구성**
- 메인 화면 진입 시 별도 메뉴 "꽁초의 일대기" 진입 가능
- 10개 액자 슬롯 (8개 일반 + 2개 히든)
- 미해금 엔딩은 실루엣 + 물음표로 표시 ("???")
- 해금 시: 엔딩 일러스트 (1장) + 엔딩 명 + 짧은 회고 문구 + 도달 조건(스포일러 가림 처리)

**예시**
```
[01] 폐 망가져 죽음          ☑ 도달 (Day 27)
[02] 전설의 골초             ☑ 도달 (Day 41)
[03] 갈매기 마피아 보스      ☐ ???
[04] 약물 과다               ☑ 도달 (Day 19)
[05] 인간 입양               ☐ ???
[06] 갑자기 철학자가 됨      ☐ ???
[07] 담배 회사 CEO와 결혼    ☐ ???
[08] 그냥 어느 날 사라짐     ☐ ???
[09] 돌고래와 떠남          🔒 (히든)
[10] 평범한 삶               🔒 (히든)
```

**누적 마일스톤 보상** (개별 엔딩 보상과 별개)
| 누적 해금 | 추가 보상 |
|----------|----------|
| 3개 | 신규 사냥터 "도시 옥상" 해금 |
| 5개 | 신규 사냥터 "지하철 입구" 해금 |
| 8개 | 신규 BGM 트랙 3종 해금 |
| 10개 (전체) | 진엔딩 "꽁초의 마지막 회고" 시네마틱 + 크레딧 컷신 + 모든 코스튬 색상 변형 해금 |

**공유 기능**
- 엔딩 해금 시 SNS 공유 카드 자동 생성 (도트 일러스트 + 엔딩명)
- 짤 만들기 좋은 포맷 → 자연 마케팅
- 워터마크에 "끄어억" 로고 + 청소년이용불가 표기

### 3.7 코스튬 시스템

엔딩 해금으로 얻는 코스튬은 **외형 변경 + 게임플레이 효과**를 동시에 제공한다. 회차 시작 시 보유 코스튬 중 하나를 선택해 출발한다.

#### 3.7.1 기본 코스튬

| 코스튬 | 외형 | 효과 | 해금 |
|--------|------|------|------|
| 기본 꽁초 | 누런 깃털, 부리에 꽁초 | 효과 없음 | 게임 시작 시 |

#### 3.7.2 엔딩 보상 코스튬 (10종)

| 코스튬 | 외형 | 효과 (시작 시 적용) | 트레이드오프 |
|--------|------|---------------------|-------------|
| 환자복 갈매기 | 병원 가운 + 링거 | 시작 폐 건강 +20 | 시작 비행 능력 -10 (약해보임) |
| 시가 마스터 | 실크 가운, 베레모 | S급 시가 1개 보유 | 시작 니코틴 욕구 +30 |
| 정장 갈매기 | 검은 정장, 선글라스 | 시작 평판 +20 | 다른 갈매기와 잡담 시 1턴 추가 소요 (위엄) |
| 좀비 갈매기 | 충혈 눈, 헝클어진 깃털 | 폐 손상 -30%, 인간이 무서워함 (관광객 강탈 성공률 +20%) | 평판 -30 (다들 피함) |
| 가정용 갈매기 | 목줄 + 이름표 | 시작 시 음식 5개 보유, 스트레스 자연 감소 +5%/일 | 인간 추격 시 도주 속도 -15% (목줄 무게) |
| 사색가 | 안경 + 책 | 스트레스 자연 감소 +10%/일, 금연 도전 난이도 -1 | 흡연 시 만족도 -20% (양심의 가책) |
| 웨딩 갈매기 | 턱시도 + 부토니에 | 시작 자금 +50%, A급 이상 담배 만족도 +15% | 일반 꽁초 만족도 -30% (입맛 고급화) |
| 안개 갈매기 | 반투명 깃털 효과 | 라이벌 갈매기 회피 가능, 인간 추격 안 당함 | 동료 갈매기와 협력 불가 (존재감 없음) |
| 항해사 갈매기 | 선원 모자 | 사냥터 "원해" 전용 접근, 비행 능력 +15 | 도시 사냥터에서 평판 패널티 -10 |
| 회사원 갈매기 | 와이셔츠 + 넥타이 | 모든 시작 스탯 +10 | 스트레스 누적 속도 +20% (직장인의 비애) |

#### 3.7.3 색상 변형 (전체 엔딩 해금 시)

10개 엔딩 모두 해금 시, 보유한 모든 코스튬에 **3가지 색상 변형**이 추가된다.
- 기본색
- 선셋 (노을 톤)
- 모노크롬 (흑백)

색상 변형은 외형만 변경, 효과는 동일.

#### 3.7.4 디자인 원칙

- **각 코스튬은 명확한 트레이드오프를 가진다**: 무료 강화 ❌
- **다른 플레이 스타일을 권유한다**: 특정 엔딩으로 가는 길을 더 쉽게 만들지 않고, *다른 길*을 보여준다
- **시각적 즉각성**: 한눈에 어떤 엔딩 해금자인지 알아볼 수 있어야 함 (스트리밍/짤 친화적)

### 3.8 진행
- 1회차: 40~90분
- 게임 내 1일 = 실시간 5분
- 평균 수명: 30일
- 회차 보상: 코스튬, 시작 보너스, 신규 사냥터, BGM

---

## 4. UI/UX 디자이너

### 4.1 비주얼
**아트**: 16비트 도트 + 일부 8비트 (의도적 거친 질감)
**팔레트**: 부둣가 회색 #6B7280 / 누런 깃털 #D4A574 / 꽁초 빨강 #C03030 / 노을 #E97451 / 바다 #2E5C6E

### 4.2 메인 화면
```
┌─────────────────────────────────────────────────┐
│ Day 15  ☁️  🌆 저녁                             │
├─────────────────────────────────────────────────┤
│                                                 │
│           [갈매기 도트, 부리에 꽁초🚬]          │
│                                                 │
├─────────────────────────────────────────────────┤
│ 🫁 폐 ▓▓▓▓░░ 60%   🚬 욕구 ▓▓▓▓▓░ 80%         │
│ ✈️ 비행 ▓▓▓░░░ 50%  😤 스트레스 ▓▓▓▓░░ 65%    │
│ 👥 평판 ▓▓░░░░ 35%                             │
├─────────────────────────────────────────────────┤
│  [사냥] [흡연] [수면] [탐험] [상호작용]        │
└─────────────────────────────────────────────────┘
```

### 4.3 시그니처 디테일

**울음소리 동적 변화**
- 100~70%: "끼룩끼룩"
- 70~40%: "끼…룩…"
- 40~10%: "끄어억"
- 10% 이하: "케헥…"

**연출 디테일**
- 부리 꽁초 연기 파티클, 재 떨어짐
- 비 오는 날 라이터 점화 미니게임
- 금단증상: 화면 떨림, 채도 감소, 식은땀
- 사망 엔딩 직전: 화면이 점점 회색

### 4.4 사운드
- BGM: 8비트 재즈 + 부둣가 앰비언스
- SFX: 라이터 "치익", 흡연 "쓰읍", 기침 5단계
- 보이스: 의성어만. 언어 없음 → 글로벌 동시 출시 가능

### 4.5 UX
- 모바일 한 손 조작
- 모든 액션 ≤ 2탭
- 사망 후 "다시 시작" 즉시 노출
- 튜토리얼은 첫 흡연까지만, 이후 자유

### 4.6 멀티플랫폼 반응형 (필수 요건)

본 게임은 **단일 HTML로 동작하므로, 브라우저가 있는 모든 기기에서 즉시 실행 가능해야 한다**. 모바일과 PC를 처음부터 동시에 고려한 반응형 설계.

**모바일 대응 해상도**
- 16:9 (구형 안드로이드, 태블릿)
- 19.5:9 (iPhone 12 이후)
- 20:9 / 21:9 (최신 안드로이드 플래그십)
- 4:3 / 3:2 (iPad, 태블릿)
- 가로/세로 모드 모두 지원 (세로 모드 우선)

**PC 대응 해상도**
- 1280×720 (최소 사양)
- 1920×1080 (Full HD, 표준)
- 2560×1440 (QHD)
- 3840×2160 (4K)
- 21:9 울트라와이드 (3440×1440)
- 32:9 슈퍼 울트라와이드 (좌우 여백 처리)
- 브라우저 창 리사이즈 시 UI 즉시 재배치 (`resize` 이벤트)
- 전체화면 모드 지원 (`Fullscreen API`)

**입력 방식 대응**
| 환경 | 입력 |
|------|------|
| 모바일 브라우저 | 터치 (한 손 조작 우선) |
| PC 브라우저 | 마우스 + 키보드 단축키 (1~5번 키로 액션) |
| 태블릿 | 터치 + 외부 키보드 (선택적) |
| 게임패드 | Gamepad API로 Xbox/PlayStation 패드 자동 인식 |

**반응형 레이아웃 규칙 (CSS 기반)**
- 스탯 게이지: `vw` / `vh` / `%` 단위 (px 고정 ❌)
- 버튼: 모바일 Safe Area (`env(safe-area-inset-*)`) 적용
- 갈매기 캐릭터: 화면 비율 변경 시에도 중앙 유지 (CSS Grid / Flexbox)
- 폰트: `clamp()` 함수로 자동 스케일
- 태블릿 / 와이드 모니터: 좌우 여백 자동 추가
- 울트라와이드: 추가 영역에 부둣가 배경 확장 (UI 늘이지 말 것)
- 미디어 쿼리: `@media (max-width: 768px)` 등으로 분기

**기기별 특수 대응**
| 기기 | 처리 |
|------|------|
| 폴더블 (Z Fold) | CSS `@media (horizontal-viewport-segments: 2)` 활용 |
| 다이나믹 아일랜드 | `env(safe-area-inset-top)` 회피 |
| 가로 모드 진입 | `orientationchange` 이벤트로 레이아웃 전환 |
| 저사양 기기 | `requestAnimationFrame` 프레임 드랍 감지 시 효과 자동 감소 |
| 4K / 고DPI | `devicePixelRatio` 기반 캔버스 스케일링 |
| 오프라인 | Service Worker로 첫 로드 후 오프라인 캐싱 (선택사항) |

---

## 5. 개발자

### 5.1 스택 (단일 HTML 파일)

**최종 산출물**: `seagull.html` 단일 파일 1개

**구성**
- HTML5 + CSS3 + Vanilla JavaScript (또는 경량 라이브러리)
- 모든 스타일 / 스크립트 / 에셋을 단일 파일에 인라인
- 외부 의존성 ❌ (CDN 호출도 ❌, 오프라인 동작 가능해야 함)
- 픽셀 아트는 인라인 SVG 또는 Base64 PNG
- 사운드는 Base64 인코딩 또는 Web Audio API 절차적 생성

**렌더링**
- HTML5 Canvas API (2D 컨텍스트)
- 또는 DOM + CSS 픽셀 아트 (단순 UI 부분)
- 스프라이트 시트 1장 → Base64로 인라인

**라이브러리 옵션** (인라인 가능한 것만)
- 순수 Vanilla JS 권장
- 필요 시 PixiJS 또는 Phaser.js를 인라인하되 파일 크기 주의

**파일 크기 목표**
| 구간 | 크기 |
|------|------|
| 미니멀 빌드 | < 2MB |
| 표준 빌드 (사운드 포함) | < 10MB |
| 절대 상한 | 30MB (브라우저 로딩 한계 고려) |

**브라우저 지원**
- Chrome / Edge / Safari / Firefox 최신 2버전
- 모바일 Safari (iOS 16+) / Chrome Android
- IE / 구형 브라우저 ❌

**배포 방식**
- itch.io 업로드 (HTML5 게임 카테고리)
- 자체 웹 호스팅 (정적 파일)
- 다운로드 후 로컬 실행도 가능 (file:// 프로토콜)
- Steam은 Electron 래핑으로 추후 검토 가능

### 5.2 아키텍처 (모듈 구조)

단일 파일이지만 IIFE 또는 ES Module 패턴으로 논리적 분리.

```javascript
// seagull.html 내 <script> 구조
(() => {
  const GameState = { /* 전역 상태 */ };

  const Modules = {
    SeagullController: { /* 갈매기 상태 관리 */ },
    StatSystem:        { /* 5대 스탯 */ },
    AddictionSystem:   { /* 중독 로직 */ },
    HealthSystem:      { /* 폐 건강 누적 */ },
    EventManager:      { /* 이벤트 트리거 */ },
    TimeManager:       { /* 시간/날씨/날짜 */ },
    InventorySystem:   { /* 담배 등급별 보유 */ },
    EndingManager:     { /* 엔딩 분기 판정 */ },
    CostumeSystem:     { /* 코스튬 효과 적용 */ },
    SaveLoadSystem:    { /* localStorage 기반 */ },
    Renderer:          { /* Canvas 렌더링 */ },
    InputHandler:      { /* 터치/마우스/키보드 통합 */ },
  };

  // 메인 루프
  function gameLoop(timestamp) {
    Modules.TimeManager.update(timestamp);
    Modules.SeagullController.update();
    Modules.Renderer.draw();
    requestAnimationFrame(gameLoop);
  }

  window.addEventListener('load', () => {
    Modules.SaveLoadSystem.load();
    requestAnimationFrame(gameLoop);
  });
})();
```

### 5.3 핵심 로직
```javascript
function smoke(grade) {
  stats.stress           -= grade.satisfaction * 0.6;
  stats.nicotineCraving  -= grade.satisfaction;
  stats.lungHealth       -= grade.lungDamage;
  stats.flightAbility     = calculateFlightFromLung(stats.lungHealth);

  if (stats.lungHealth <= 0) triggerEnding('LUNG_DEATH');
  if (stats.nicotineCraving > 90) triggerOverdoseRisk();

  updateSeagullVoice(stats.lungHealth);
  updateVisualEffects(stats);
}
```

### 5.4 리스크
- 청소년이용불가 등급 가능성 높음 → 그냥 청불로 갈 것. 표현 자기검열로 톤 망치지 말 것.
- 단일 HTML 파일 크기 비대화 → 에셋 압축 / 절차적 사운드 생성으로 관리
- 브라우저 정책 변경 (오디오 자동재생 차단 등) → 사용자 첫 클릭 후 사운드 활성화 패턴 적용
- `localStorage` 5MB 한도 → JSON 압축 또는 IndexedDB 전환 대비
- 사용자 캐시 삭제로 데이터 유실 → Import/Export 기능으로 사용자 백업 책임 위임

### 5.5 산출물 의무 사항

**🚨 게임 빌드가 만들어지면 반드시 `게임 디자인.md` 파일을 생성해야 한다.**

이는 선택 사항이 아니다. 게임이 출시 가능한 상태에 도달하면 다음 내용을 포함한 `게임 디자인.md`를 프로젝트 루트에 생성하여 함께 제출한다. 최종 산출물은 **`seagull.html` + `게임 디자인.md` 2개 파일**.

**필수 포함 항목**
- 최종 확정된 핵심 게임 루프
- 5대 스탯 시스템의 최종 수치 (밸런싱 결과 반영)
- 담배 등급별 최종 효과량 표
- 10개 엔딩의 최종 트리거 조건 (개발 과정에서 변경된 것 반영)
- 엔딩별 코스튬 보상 + 누적 마일스톤 보상 최종 명세
- 코스튬 시스템 최종 효과 / 트레이드오프
- 단일 HTML 파일 구조 (모듈 분리 방식)
- 브라우저 호환성 매트릭스
- localStorage 키 네이밍 규칙
- 출시 시점 기준 알려진 이슈 / 의도된 동작
- 다음 버전 계획 (있다면)

**관리 규칙**
- Git 저장소에 함께 커밋
- 패치마다 갱신
- 신규 합류자가 이 문서 하나만 읽어도 게임 전체 구조 파악 가능해야 함

### 5.6 세이브/로드 시스템 (필수 명세)

본 게임은 단일 HTML + 로그라이트 구조이므로 세이브 데이터는 **회차 데이터 ≠ 메타 데이터**로 명확히 분리되어야 한다. 모든 저장은 **브라우저 스토리지 API**를 사용한다.

#### 5.6.1 데이터 구조

**런 데이터 (현재 회차)** — `localStorage` 키: `kkeuh_run_{slotIndex}`
- 갈매기 현재 스탯 (5종)
- 현재 일차 / 시간대 / 날씨
- 인벤토리 (담배 등급별 보유량)
- 발생한 이벤트 플래그
- 현재 평판 / 라이벌 관계 상태
- 적용된 코스튬 ID
- 슬롯 3개 (`kkeuh_run_0`, `kkeuh_run_1`, `kkeuh_run_2`)

**메타 데이터 (영구 보존)** — `localStorage` 키: `kkeuh_meta`
- 해금된 엔딩 목록 (10개 비트마스크)
- 해금된 코스튬 / 사냥터 / BGM 목록
- 누적 통계 (총 회차 수, 총 흡연량, 최장 수명, 도달 엔딩 횟수)
- 게임 설정 (사운드, UI 스케일, 언어)
- 회차 종료 시에도 절대 삭제 ❌

**스토리지 선택**
| 데이터 종류 | 저장소 | 이유 |
|------------|--------|------|
| 런 / 메타 (텍스트 JSON) | `localStorage` | 5MB 한도 내 충분, 동기 API 단순 |
| 사용자 캡처 / 큰 데이터 (있다면) | `IndexedDB` | 50MB+ 가능, 비동기 |
| 임시 세션 데이터 | `sessionStorage` | 탭 닫으면 자동 삭제 |

#### 5.6.2 저장 시점

| 시점 | 자동/수동 | 대상 | 트리거 이벤트 |
|------|----------|-----|--------------|
| 하루 종료 (잠들 때) | 자동 | 런 + 메타 | 게임 내 이벤트 |
| 모바일 탭 비활성화 | 자동 (즉시) | 런 | `visibilitychange` |
| PC 창 닫기 / 새로고침 | 자동 | 런 | `beforeunload` |
| 엔딩 도달 시 | 자동 | 메타 | 게임 내 이벤트 |
| 메뉴에서 수동 저장 | 수동, 언제든 가능 | 런 | UI 버튼 |
| 5분마다 백그라운드 | 자동 | 런 (안전망) | `setInterval` |

#### 5.6.3 세이브 슬롯 정책

**철학: 플레이어가 편하게 엔딩을 탐색할 수 있어야 한다.**

이 게임은 액션 실력으로 죽음을 회피하는 게임이 아니다. 핵심 재미는 **다양한 엔딩 분기를 시도해보는 것**이며, 세이브/로드는 그 탐색을 돕는 도구다.

- **런 슬롯: 3개 제공**
  - 슬롯마다 다른 갈매기를 동시에 키울 수 있음
  - 분기 탐색용 백업 슬롯으로 활용 가능
  - 각 슬롯별로 세이브 시점 / 갈매기 정보 / 일차 / 코스튬 미리보기 표시

- **수동 세이브/로드 자유**
  - 언제든지 세이브 가능
  - 죽음 직전으로 되돌아가는 것도 허용
  - 분기 선택 후 다른 분기 시도하는 것도 허용

- **메타 슬롯: 브라우저별 1개** (`kkeuh_meta`)
  - 같은 브라우저 + 같은 도메인 내에서만 공유
  - 시크릿 모드 / 브라우저 변경 / 다른 기기 = 별개 메타
  - 엔딩 앨범, 코스튬, 누적 통계 영구 보존

- **신규 회차 시작**
  - 비어있는 슬롯이 있으면 바로 새 갈매기 시작
  - 모든 슬롯이 차있으면 덮어쓸 슬롯 선택 (확인 다이얼로그)

#### 5.6.4 무결성 / 안전성

- **체크섬 검증**: JSON에 `checksum` 필드 추가, 로드 시 재계산 비교 → 변조 시 백업본 복원
- **자동 백업**: 각 슬롯의 직전 세이브 1개 보관 (`kkeuh_run_0_bak` 등)
- **JSON 파싱 실패 대응**: try/catch로 보호, 실패 시 백업본 시도 → 모두 실패 시 신규 시작 안내
- **버전 마이그레이션**: 메타에 `schemaVersion` 필드, 게임 업데이트 시 자동 변환 로직 필수
- **스토리지 풀 대응**: `QuotaExceededError` 발생 시 사용자에게 알림 + 오래된 백업 삭제

#### 5.6.5 데이터 휴대성 (Import/Export)

브라우저 스토리지는 사용자가 캐시 삭제 시 같이 날아간다. 이를 방지하기 위한 수동 백업 기능 제공:

- **내보내기 버튼**: 메타 데이터를 Base64 문자열로 클립보드에 복사 또는 `.txt` 파일로 다운로드
- **가져오기 버튼**: 텍스트 입력창에 붙여넣기 → 검증 후 메타 복원
- **파일명 규칙**: `kkeuh_save_YYYYMMDD.txt`
- **공식 클라우드 동기화 ❌**: 단일 HTML이므로 서버 없음. 사용자 책임으로 백업

#### 5.6.6 의사코드

```javascript
const SaveLoadSystem = {
  saveRun(slotIndex) {
    const data = {
      ...gameState.run,
      timestamp: Date.now(),
      schemaVersion: 1,
    };
    data.checksum = computeChecksum(data);

    try {
      // 직전 세이브를 백업으로 이동
      const prev = localStorage.getItem(`kkeuh_run_${slotIndex}`);
      if (prev) localStorage.setItem(`kkeuh_run_${slotIndex}_bak`, prev);

      localStorage.setItem(`kkeuh_run_${slotIndex}`, JSON.stringify(data));
    } catch (e) {
      if (e.name === 'QuotaExceededError') notifyStorageFull();
    }
  },

  saveMeta() {
    const data = { ...gameState.meta, schemaVersion: 1 };
    data.checksum = computeChecksum(data);
    localStorage.setItem('kkeuh_meta', JSON.stringify(data));
  },

  loadRun(slotIndex) {
    const raw = localStorage.getItem(`kkeuh_run_${slotIndex}`);
    if (!raw) return null;
    try {
      const data = JSON.parse(raw);
      if (!verifyChecksum(data)) return loadBackup(slotIndex);
      return migrateIfNeeded(data);
    } catch {
      return loadBackup(slotIndex);
    }
  },

  onSeagullEnding(ending) {
    gameState.meta.unlockEnding(ending);
    gameState.meta.unlockCostume(ending.costumeId);
    gameState.meta.updateStats(gameState.run);
    this.saveMeta();
    showEndingScreen(ending);
    // 런 데이터는 유지 → 직전 시점으로 로드해 다른 엔딩 시도 가능
  },

  exportMeta() {
    const raw = localStorage.getItem('kkeuh_meta') || '{}';
    return btoa(raw);  // Base64 인코딩
  },

  importMeta(base64String) {
    try {
      const raw = atob(base64String);
      const data = JSON.parse(raw);
      if (!verifyChecksum(data)) throw new Error('체크섬 불일치');
      localStorage.setItem('kkeuh_meta', raw);
      return true;
    } catch {
      return false;
    }
  },
};

// 자동 저장 이벤트 바인딩
window.addEventListener('visibilitychange', () => {
  if (document.hidden) SaveLoadSystem.saveRun(currentSlot);
});
window.addEventListener('beforeunload', () => {
  SaveLoadSystem.saveRun(currentSlot);
});
setInterval(() => SaveLoadSystem.saveRun(currentSlot), 5 * 60 * 1000);
```

---

## 6. QA

### 6.1 기능 테스트
- 5대 스탯 변화량 정확성
- 10개 엔딩 모두 트리거 가능
- 시간대별 이벤트 정상 발생

**세이브/로드 (별도 강조)**
- 런 슬롯 3개 동시 운용 검증 (각 슬롯 독립성)
- 런 데이터와 메타 데이터 분리 저장 검증
- 엔딩 도달 후 직전 시점으로 로드 → 다른 엔딩 시도 가능 여부
- 모바일 백그라운드 진입 → 즉시 복귀 시 진행상황 유지
- PC 윈도우 강제 종료 후 재시작 시 자동 백업 복원
- 게임 버전 업데이트 시 구버전 세이브 마이그레이션
- 클라우드 동기화 충돌 시나리오 (기기 2개 동시 사용)
- 세이브 파일 손상 시 백업본 자동 복원
- 슬롯 덮어쓰기 시 확인 다이얼로그 정상 작동

### 6.2 밸런스
- 평균 수명 28~35일
- 금연 도전 성공률 30~40%
- 담배 등급별 가성비 적절 분포
- 1회차에 우연히 좋은 엔딩 안 나오게

### 6.3 호환성 (브라우저 기반)

**브라우저**
- Chrome / Edge: 최신 2버전
- Safari: macOS 14+ / iOS 16+
- Firefox: 최신 2버전
- Samsung Internet (Android 주력 브라우저)
- 모바일 인앱 브라우저 (카카오톡 / 인스타그램 내장 브라우저) 동작 확인

**모바일 환경**
- iOS 16+ Safari, iOS Chrome
- Android 12+ Chrome, Samsung Internet
- 16:9 ~ 19.5:9 / 폴더블 / 태블릿
- 가로/세로 회전 시 레이아웃 정상 전환

**PC 환경**
- Windows 10/11, macOS 12+, Linux (Chrome/Firefox)
- 1280×720 ~ 4K / 울트라와이드 32:9 까지
- 브라우저 창 리사이즈 시 UI 깨짐 검증
- 전체화면 모드 (`Fullscreen API`) 동작 확인
- 마우스 + 키보드 / 게임패드 (Gamepad API) 입력

**스토리지 환경**
- 시크릿 모드에서도 단일 세션 동안 정상 동작
- `localStorage` 비활성 환경 감지 시 안내 메시지
- 캐시 삭제 후 재진입 시 graceful 처리

**파일 크기 / 로딩**
- 초기 로딩 < 5초 (3G 환경 기준)
- 표준 빌드 < 10MB
- 오프라인 동작 검증 (다운로드 후 file:// 실행)

### 6.4 UX
- 신규 유저 첫 5분 이탈률
- 튜토리얼 명확성 (3차 사용자 테스트)
- 모바일 한 손 조작
- 색맹 모드 (스탯 게이지)

### 6.5 심의 (실무 한정)
- 실제 브랜드명/로고 무단 사용 여부 최종 확인 ("말보로" 같은 거 다 가공명으로)
- 게임위 청소년이용불가 등급 신청 (자체등급분류 사업자 또는 직접 신청)
- 청소년이용불가 마크가 모든 노출 지점에 정상 표시되는지 확인 (itch.io / 자체 페이지 / 트레일러 / SNS / 굿즈 / 공유 카드)
- 18세 이상 확인 다이얼로그 첫 진입 시 정상 노출 (웹 게임 한계상 강제 차단 불가)
- itch.io 페이지 흡연/중독 묘사 표기 (성인 콘텐츠 토글)
- 그 외 자기검열 항목 없음

### 6.6 버그 우선순위
| 등급 | 정의 | 대응 |
|------|------|------|
| P0 | 진행 불가 / 크래시 | 즉시 핫픽스 |
| P1 | 엔딩 분기 오류 / 결제 | 24시간 내 |
| P2 | 밸런스 / 시각 깨짐 | 정기 패치 |
| P3 | 오타 / 미세 애니메이션 | 마이너 패치 |

### 6.7 출시 후
- 첫 7일 크래시율 < 1%
- 평균 플레이타임 / 회차 횟수
- 가장 많이 도달하는 엔딩 통계 → 밸런싱
- 리뷰 키워드 분석

---

## 📌 한 줄 요약

> **"꽁초를 문 갈매기 한 마리가 부두에 산다. 그는 죽거나, 살거나, 사라진다. 어느 쪽이든 부두는 그대로다."**

— 끝 —
